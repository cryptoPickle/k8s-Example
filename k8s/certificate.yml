apiVersion: certmanager.k8s.io/v1alpha1
kind: Certificate
metadata:
  name: k9s-multi-com-tls
spec:
  secretName: k8s-multi-com
  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer
  commonName: recursive.online
  dnsNames:
    - recursive.online
    - www.recursive.online